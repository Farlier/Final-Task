@model IEnumerable<WebApplication.Models.QualityClassViewModel>
<div class="baner row">
    <h1 class="col-12 col-sm-12 col-md-12 col-lg-12 col-xl-12 text-center" style="padding-top:100px; padding-bottom:50px;">Арендуй авто за пару минут!</h1>
    @*<div class="col-12 col-sm-12 col-md-12 col-lg-12 col-xl-12 text-center "

        @using (Html.BeginForm("GetConfig", "Car" ,FormMethod.Post,new { @class = "text-center row justify-content-center" }))
        {

            <div class="form-group" style="margin-right:20px">
                <label class="control-label "> Город </label>
                <input id="searchcity "class="form-control" type="text" name="City" />
            </div>

            @*<div class="form-group" style="margin-right:20px">
                <label class="control-label "> Начало проката </label>
                <input class="form-control" type="date" name="DateStart" />
            </div>

            <div class="form-group" style="margin-right:20px">
                <label class="control-label"> Конец проката </label>
                <input class="form-control" type="date" name="DateEnd" />
            </div>

            <div class="w-100" style="padding-bottom:20px;"></div>
            <input class="btn btn-primary" style="margin:auto;" type="submit" value="Отправить" />
        }
    </div>
        *@
</div>
<div class="row justify-content-center" style="margin-top:20px;">
    @foreach (var it in Model)
    {
        <a class="col-10 col-sm-4 col-md-3 col-lg-3 col-xl-3 car my-no-gutters text-decoration-none" 
           style="background-color:lightgray; margin:15px; color:black"  
            href="@Url.Action("SelectionResult","CarModel",new { qualityClass = it.Name })">

            <div class=" my-no-gutters " >
                <div class="text-center" style="width:100%; background-color:gray; color:orange">
                    Класс @it.Name
                </div>
                <div class="image" style="background-image:url(@it.Photo);"></div>
                <p class="text-center">
                    @it.Description
                </p>
            </div>
        </a>
    }
</div>
